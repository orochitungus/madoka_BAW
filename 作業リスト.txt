覚醒カットインをuguiを使うように変える

状態制御にAnimatorを使うように切り替える（可能かどうか解らないので、別ブランチを作って実験する）

ほむらの覚醒技の歩行演出中はロックオンカーソルを出さないようにする

スコノの高跳びをさせないようにする（一定高度以上まで上がると、一発撃って下特殊格闘を入れる）
スコノの近距離で前格闘連打を修正する
覚醒演出中は覚醒をさせないようにする（敵味方問わず）

魔獣でロックオンしている相手がダウンしたときは、別の相手をロックするか、ロックする相手がいないときは一旦バックさせるようにする

慣性ジャンプを出来るようにする（ボタンを離して再入力するとジャンプできるようにする）
ステップ距離と移動速度を調整する
横ステップで相手の周りを回るようにする（横に回り込めるようにする）
