キャラクターアニメーションの状態制御にAnimatorを使うように切り替える（可能かどうか解らないので、別ブランチを作って実験する）
CPUの動作遷移もAnimatorの状態遷移を元に判定する

スコノの高跳びをさせないようにする（一定高度以上まで上がると、下特殊格闘を入れる）
スコノの近距離で前格闘連打を修正する（CPUの格闘に横格闘と前格闘を入れ、終わったらCPUMODEをNORMALに戻す）
覚醒演出中は覚醒をさせないようにする（敵味方問わず）


慣性ジャンプを出来るようにする（ボタンを離して再入力するとジャンプできるようにする）
ステップ距離と移動速度を調整する(速度を早くして距離を短く）
横ステップで相手の周りを回るようにする（横に回り込めるようにする。通常ロック時の横歩行と同じ移動をする）

エキストラを作る（入院患者・医者・看護師２タイプ程度）

56Fにエキストラを配置する

アイテムを配置し、入手可能にする

病院前,1F,106Fと150Fへ行けるようにする

サブイベント１を実装する

鹿目家全景を作る

美樹家全景を作る

ホームページを更新する

見滝原市全景を作る


