ステップ実行手順

AnimationState_Idle
AnimationState_Jumping
AnimationState_Fall
AnimationState_Run

上記の状態で入力成功するとStepDoneが実行されてステップの処理に映る
moveDirectionの値はそのフレームの方向キー（入力の都合で最低何かしら入っている・・・アレ？CPUにステップ入力させたとき、不具合でるんじゃね？）
CPUの場合は左ステップか右ステップというコマンドを用意する
引数の一つとしてStepCheckが入る（どの方向へ飛ぶか）

StapDone

エフェクトを生成

入力の方向を計算

ロックオンしている相手の方向に向ける

アニメーションを再生する

ブーストを消費する

ステップ累積距離を初期化する

重力を無効化する

UpdateRotation_Stepを実行し、移動方向を決定する

キャンセルステップは入力の関係でMoveDirectinが相手の方向を向いているため、MoveDirectionを再設定する





