基本は立ち射撃と同じ（移動中にコマンドが完成すると、ジャンプして撃った時と同じで強制的に止まる。キャンセルダッシュはOK）

ボタン操作関係
	累積押し時間（射撃）をカウントする設定（変数/関数）	→OK
	累積押し時間（格闘）をカウントする設定（変数/関数）	→OK
	1Fあたりのゲージ増加量＆減衰量（加算・減算はUpdateCoreで。量はFirstsettingで決定。キャラごとに変えたいならキャラごとにそのあとで）		→OK


描画関連（残弾数描画に追加。射撃チャージは1段目に描画、格闘チャージは2段目に描画）
	チャージ量に応じてゲージを描画する機能			→OK
	

射撃関係
	射撃MAXで、かつボタンが離されたときに射撃(ShotDoneに追加かChargeShotDoneを作成）。優先度は射撃より高くすること				→OK
		↓
	AnimationState.ChargeShotを再生(クロスフェード)		
	m_AnimState[0] = AnimationState.ChargeShotへ変更	→OK
		↓
	10F目でShootを実行（キャラごとに異なる）		→OK
	・矢の出現ポジションをフックと一致させる
	・矢を出現させる
	・親子関係を再設定する(=矢をフックの子にする）
	・いったん止める
		↓
	30F目でShoot2を実行					→OK
	・この時点では親子なので、Homura_ChargeShotAllowsを持ったスクリプトを拾う(デフォルトで持っていないので、スクリプトで検索するしかない）
	矢のスクリプトの速度を設定する
 	矢の方向を決定する(本体と同じ方向に向けて打ち出す。ただしノーロックで本体の向きが0のときはベクトルが0になるので、このときだけはカメラの方向に飛ばす）
		ロックオン
			ロックオン対象の座標を取得
			対象の座標を取得
			本体の回転角度を取得
		非ロックオン（角度0）
			本体角度が0の場合カメラの方向を射出角とし、正規化して代入する
			ただしそのままだとカメラが下を向いているため、一旦その分は補正する
		非ロックオン（角度0以外）
			本体角度を射出角へ変更


	矢のフックの位置に弾の位置を代入する
 	
	同じく回転角を代入する

	攻撃力を決定する(ここの0がスキルのインデックス。下も同様）
	ダウン値を決定する
	固定状態を解除
	チャージ量を空にする

LateUpdate							→OK
	アニメーション終了処理判定

	地上→Idleに変更
	空中→Fallに変更

